# Story 12.1 — Stripe Webhook → Supabase (Vendas em Tempo Real)

## Status: Pending

## Contexto
Integrar o Stripe ao CRM Empresa via webhooks — cada venda, pagamento recorrente ou cancelamento aparece automaticamente no dashboard financeiro da empresa em tempo real.

## Acceptance Criteria

- [ ] AC1: Supabase Edge Function receptora de webhooks do Stripe
- [ ] AC2: Eventos processados: `checkout.session.completed`, `invoice.paid`, `customer.subscription.deleted`
- [ ] AC3: Cada evento salvo na tabela `company_revenue` com: valor, produto, cliente, status, data
- [ ] AC4: Validação de assinatura do webhook (Stripe-Signature header)
- [ ] AC5: Dados visíveis no CRM Empresa em tempo real (Story 12.3)
- [ ] AC6: Log de webhooks recebidos para debug

## Schema Supabase

```sql
CREATE TABLE company_revenue (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  source TEXT NOT NULL, -- 'stripe' | 'hotmart'
  event_type TEXT NOT NULL,
  amount DECIMAL(10,2),
  currency TEXT DEFAULT 'BRL',
  product_name TEXT,
  customer_name TEXT,
  customer_email TEXT,
  status TEXT, -- 'paid' | 'refunded' | 'cancelled'
  external_id TEXT UNIQUE, -- Stripe charge ID / Hotmart transaction ID
  metadata JSONB,
  occurred_at TIMESTAMPTZ NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE TABLE webhook_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  source TEXT NOT NULL,
  event_type TEXT,
  payload JSONB,
  processed BOOLEAN DEFAULT false,
  error TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

## Arquivos a Criar

- `supabase/functions/stripe-webhook/index.ts` — Edge Function receptora
- `src/components/company/revenue/StripeSettings.tsx` — configurar webhook secret

## Tarefas

- [ ] Criar Edge Function `stripe-webhook` com validação de assinatura
- [ ] Migration Supabase: tabelas `company_revenue` e `webhook_logs`
- [ ] Processar eventos: `checkout.session.completed`, `invoice.paid`, `subscription.deleted`
- [ ] Criar `StripeSettings.tsx` para inserir o webhook secret
- [ ] Testar com Stripe CLI: `stripe listen --forward-to [edge-function-url]`
- [ ] Commit: `feat: Stripe webhook para CRM Empresa (Story 12.1)`

## File List
(a preencher durante implementação)

## Dev Notes
Usar `stripe.webhooks.constructEvent(body, signature, secret)` para validar. Edge Function recebe raw body — não fazer JSON.parse antes da validação. Stripe CLI facilita testes locais.
